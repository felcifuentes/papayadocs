import { IMessage } from './IMessage';
export declare class MessageUtilities {
    static toPayloadJson(messages: IMessage[]): string;
    static processMessagesForTypes(messages: IMessage[], types: {
        [key: string]: (message: IMessage) => void;
    }): void;
}
