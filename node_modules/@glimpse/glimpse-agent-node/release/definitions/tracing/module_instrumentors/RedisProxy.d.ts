/**
 * A proxy for the [`redis`](https://www.npmjs.com/package/redis) package.
 */
export declare class RedisProxy {
    proxyModule(originalModule: any): any;
    private publishPre(address, commandObj, cb, correlationId);
    private publishPost(duration, address, commandObj, err, result, cb, correlationId);
}
export default RedisProxy;
