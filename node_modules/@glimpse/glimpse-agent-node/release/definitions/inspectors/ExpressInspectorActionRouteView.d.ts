import { IAgentBroker } from '../messaging/IAgentBroker';
import { IContextManager } from '../messaging/IContextManager';
import { IDateTime } from '../configuration/IDateTime';
import { IErrorReportingService } from '@glimpse/glimpse-common';
export declare class ExpressInspectorActionRouteView {
    private broker;
    private contextManager;
    private dateTime;
    private errorReportingService;
    init(broker: IAgentBroker, contextManager: IContextManager, dateTime: IDateTime, errorReportingService: IErrorReportingService): void;
    private onRouteDispatch(route, response);
    private onResponseEnd(response);
    private onRenderCompleteError(err, response);
    private onRenderComplete(response);
    private onResponseRender(response);
    private onViewRender(view);
    private sendActionViewFound(viewName, viewPath, viewFound, context);
}
