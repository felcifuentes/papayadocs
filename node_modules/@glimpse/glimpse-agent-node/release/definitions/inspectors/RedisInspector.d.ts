import { IAgent } from '../IAgent';
import { IErrorReportingService } from '@glimpse/glimpse-common';
export declare class RedisInspector {
    private agent;
    private errorReportingService;
    private stackHelper;
    private messageConverter;
    private contextManager;
    private listeners;
    init(agent: IAgent, errorReportingService: IErrorReportingService): void;
    removeEventListeners(): void;
    private onStartCommand(event);
    private onEndCommand(event);
    private coreCreateMessage<T>(types);
    private corePublish<T>(message, payload, index?);
}
